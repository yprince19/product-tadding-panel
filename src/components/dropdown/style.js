import styled from "styled-components";

export const Div = styled.div`
  .myControlClassName, .myPlaceholderClassName {
    width: 100%;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    font-size: 0.7rem;
  }
`;
